name: ZeroOps
on:
  push:
jobs:
  actions:
    env:
      PROJECT_NAME: ZeroOps
      AUTHOR: k
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run : echo "$PROJECT_NAME by $AUTHOR"

