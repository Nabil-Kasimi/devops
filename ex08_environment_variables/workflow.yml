name: ZeroOps
on:
  push:
jobs:
  actions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run : echo "$PROJECT_NAME by $AUTHOR"
    env:
      - PROJECT_NAME: ZeroOps
      - AUTHOR: k
