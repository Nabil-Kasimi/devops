name: worlkFlow
on:
  push:
jobs:
  secrets:
    runs-on: ubuntu-latest
    env:
      API_KEY: ${{secrets.API_KEY}}
    steps:
      - uses: actions/checkout@v4
      - run: echo "Key length :${#API_KEY}"
